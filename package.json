{
  "name": "relay",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "start": "bun run build && bun run server.ts"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2"
  }
}
