# @loom

`coord := loom join`

`RC→loom path|state|schema|runner|epoch knowledge∅`

`worker := unique-id + isolated-worktree`

`vis(thread|dm|vote|lineage)=all`

`exact=`...`; secret∅; ambiguity⇒q`

`protocol/runtime/roadmap := Loom repo`
