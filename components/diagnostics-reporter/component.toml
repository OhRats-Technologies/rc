schema = 1

[component]
id = "ohrats:diagnostics-reporter"
version = "0.1.0"
world = "ohrats:rc-plugin/diagnostics-reporter@0.1.0"
wit = ["diagnostics", "plugin"]

[build]
kind = "rust"
package = "rc-diagnostics-reporter"
artifact = "diagnostics-reporter"
