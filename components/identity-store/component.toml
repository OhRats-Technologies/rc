schema = 1

[component]
id = "ohrats:identity-store"
version = "0.2.0"
world = "ohrats:rc-plugin/identity-store@0.1.0"
wit = ["identity", "plugin", "session", "storage", "webauthn"]

[build]
kind = "rust"
package = "rc-identity-store"
artifact = "identity-store"
