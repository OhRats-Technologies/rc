schema = 1

[component]
id = "ohrats:api-credential-store"
version = "0.1.0"
world = "ohrats:rc-plugin/api-credential-store@0.1.0"
wit = ["api-credentials", "identity", "plugin", "storage"]

[build]
kind = "rust"
package = "rc-api-credential-store"
artifact = "api-credential-store"
