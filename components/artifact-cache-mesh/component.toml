schema = 1

[component]
id = "ohrats:artifact-cache-mesh"
version = "0.1.0"
world = "ohrats:rc-plugin/artifact-cache-mesh@0.1.0"
wit = ["artifact-cache", "plugin"]

[build]
kind = "rust"
package = "rc-artifact-cache-mesh"
artifact = "artifact-cache-mesh"
