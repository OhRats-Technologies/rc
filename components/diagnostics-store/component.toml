schema = 1

[component]
id = "ohrats:diagnostics-store"
version = "0.1.0"
world = "ohrats:rc-plugin/diagnostics-store@0.1.0"
wit = ["diagnostics", "plugin"]

[build]
kind = "rust"
package = "rc-diagnostics-store"
artifact = "diagnostics-store"
