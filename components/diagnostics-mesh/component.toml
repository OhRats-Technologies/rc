schema = 1

[component]
id = "ohrats:diagnostics-mesh"
version = "0.1.0"
world = "ohrats:rc-plugin/diagnostics-mesh@0.1.0"
wit = ["diagnostics", "mesh-diagnostics", "plugin"]

[build]
kind = "rust"
package = "rc-diagnostics-mesh"
artifact = "diagnostics-mesh"
