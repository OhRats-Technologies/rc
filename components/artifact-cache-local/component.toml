schema = 1

[component]
id = "ohrats:artifact-cache-local"
version = "0.1.0"
world = "ohrats:rc-plugin/artifact-cache-local@0.1.0"
wit = ["artifact-cache", "plugin"]

[build]
kind = "rust"
package = "rc-artifact-cache-local"
artifact = "artifact-cache-local"
